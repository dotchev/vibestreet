version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      # One grouped PR for all action bumps instead of one PR per action.
      actions:
        patterns: ["*"]
    commit-message:
      prefix: "ci"
